During 在 the 该 systems 系统 analysis 分析 phase 阶段, , you 你 must 必须 decide 决定 how 如何 data 数据 will 将 be 被 organized 组织, , stored 存储, , and 和 managed 管理. .
A 一个 ______ ______ (71) (71) ______ ______ is 是 a 一个 framework 框架 for 用于 organizing 组织, , storing 存储, , and 和 managing 管理 data 数据. .
Each 每个 file 文件 or 或 table 表 contains 包含 data 数据 about 关于 people 人, , places 地点, , things 事物, , or 或 events 事件. .
One 一个 of 的 the 该 potential 潜在 problems 问题 existing 存在 in 在 a 一个 file 文件 processing 处理 environment 环境 is 是 ______ (72) ______, , which 这 means 意味着 that 那 data 数据 common 通用 to 于 two 两个 or 或 more 更多 information 信息 systems 系统 is 被 stored 存储 in 在 several 多个 places 地方. .

(空一行)

In 在 a 一个 DBMS 数据库管理系统中, , the 该 linked 链接 tables 表 form 形成 a 一个 unified 统一 data 数据 structure 结构 that 这 greatly 大大 improves 提高 data 数据 quality 质量 and 和 access 访问. .
A(n) 一个 ______ (73) ______ is 是 a 一个 model 模型 that 它 shows 展示 the 该 logical 逻辑 relationships 关系 and 和 interaction 交互 among 在……之间 system 系统 entities 实体. .
It 它 provides 提供 an 一个 overall 整体 view 视图 of 的 the 该 system 系统 and 以及 a 一个 blueprint 蓝图 for 用于 creating 创建 the 该 physical 物理 data 数据 structures 结构. .
______ (74) ______ is 是 the 该 process 过程 of 的 creating 创建 table 表 designs 设计 by 通过 assigning 分配 specific 特定 fields 字段 or 或 attributes 属性

to 到 each 每个 table 表 in 在 the 该 database 数据库. .
A 一个 table 表 design 设计 specifies 指定 the 该 fields 字段 and 并 identifies 标识 the 该 primary 主 key 键 in 在 a 一个 particular 特定 table 表 or 或 file 文件. .
The 该 three 三个 normal 范式 forms 形式 constitute 构成 a 一个 progression 递进 in 在 which 其中 ______ (75) ______ represents 代表 the 该 best 最佳 design 设计. .
Most 大多数 business-related 与业务相关的 databases 数据库 must 必须 be 被 designed 设计 in 采用 that 那 form 形式. .

(71)
A. data entity 数据实体
B. data structure 数据结构 ✓
C. file collection 文件集合
D. data definition 数据定义

(72)
A. data integrity 数据完整性
B. the rigid data structure 刚性数据结构
C. data redundancy 数据冗余 ✓
D. the many-to-many relationship 多对多关系

(73)
A. entity-relationship diagram 实体-关系图 ✓
B. data dictionary 数据字典
C. database schema 数据库模式
D. physical database model 物理数据库模型

(74)
A. Normalization 规范化 ✓
B. Replication 复制
C. Partitioning 分区
D. Optimization 优化

(75)
A. standard notation form 标准表示法形式
B. first normal form 第一范式
C. second normal form 第二范式
D. third normal form 第三范式 ✓